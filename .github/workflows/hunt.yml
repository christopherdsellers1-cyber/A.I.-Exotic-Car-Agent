name: Porschebot-Agent

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *' # Runs every hour to find equity first

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true' # Silences deprecation warnings

jobs:
  hunt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pandas

      - name: Run Agent
        run: python main_agent.py
